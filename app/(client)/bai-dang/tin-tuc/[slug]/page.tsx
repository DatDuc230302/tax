import React from 'react';

export default function Page({ params }: { params: { slug: string } }) {
    return (
        <div className="w-full flex justify-center h-[4000px]">
            <div className="w-[1200px] flex">Dat</div>
        </div>
    );
}
